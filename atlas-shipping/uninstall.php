<?php
if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) {
    exit;
}

// ATLAS Shipping preserves company data, plugin options, activity records,
// and the application page by default. Destructive cleanup is intentionally
// not implemented in version 0.1.3.
